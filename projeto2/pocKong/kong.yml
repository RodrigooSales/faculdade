_format_version: "3.0"
_transform: true

services:
- host: httpbin.org
  name: example_service
  port: 80
  protocol: http
  routes:
  - name: example_route
    paths:
    - /mock
    strip_path: true
- host: localhost
  name: criacao_servico
  port: 8080
  protocol: http
  routes:
  - name: rota_raiz
    paths:
    - /
    strip_path: true

- host: localhost
  name: recintos_servico
  port: 8081
  protocol: http
  routes:
  - name: rota_raiz
    paths:
    - /
    strip_path: true

- host: localhost
  name: individuos_servico
  port: 8081
  protocol: http
  routes:
  - name: rota_raiz
    paths:
    - /
    strip_path: true


